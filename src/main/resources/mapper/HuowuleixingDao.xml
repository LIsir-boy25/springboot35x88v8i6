<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yb.dao.HuowuleixingDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.yb.entity.HuowuleixingEntity" id="huowuleixingMap">
        <result property="huowuleixing" column="huowuleixing"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.yb.entity.vo.HuowuleixingVO" >
		SELECT * FROM huowuleixing  huowuleixing         
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.yb.entity.vo.HuowuleixingVO" >
		SELECT  huowuleixing.* FROM huowuleixing  huowuleixing 	
 		<where>${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.yb.entity.view.HuowuleixingView" >

		SELECT  huowuleixing.* FROM huowuleixing  huowuleixing 	        
        <where>${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.yb.entity.view.HuowuleixingView" >
		SELECT * FROM huowuleixing  huowuleixing <where>${ew.sqlSegment}</where>
	</select>
	


</mapper>
